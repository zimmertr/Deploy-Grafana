apiVersion: 1
providers:
- name: 'Hardware'
  org_id: 1
  folder: 'Hardware'
  type: 'file'
  editable: true
  options:
    folder: '/etc/grafana/provisioning/dashboards/Hardware'
- name: 'General'
  org_id: 1
  # folder: 'General' # General folder exists by default, providing `folder` directive breaks Grafana.
  type: 'file'
  editable: true
  options:
    folder: '/etc/grafana/provisioning/dashboards/General'
